# Venue badges for bibliography entries that set `abbr={...}` in papers.bib.
# Each key is the abbr value; `url` and `color` are both optional.
# No entry currently uses abbr, so this file is intentionally empty.
